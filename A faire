2 - Ajouter le Transformateur, decodeur, avec plusieurs contextes
4 - Tester avec Interv=1,8

Optimisations :
	(Faire le system de teste de chaque instruction)

	/!\ IMPORTANT : Optimiser :
		Batch Norm,
		Softmax,
		QKtDivClef

		matmul

Modèles :
	Decodeur, CTX0, CTX1, CTX2 ...

	Self-Encodeur -> Transformers (qui prennent un vecteur null en entrée) -> Linéair